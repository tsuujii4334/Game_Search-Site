<div class="container px-5,py-10">
  <div class="row">
    <div class="col">
      <table class="table table-hover table-inverse bg-white">
        <h2 class="bg-white">
          <!--検索対象モデルがGameの時 -->
          <% if @range == "Game" %>
            検索ワード "<%= @word %>"
          <!--検索対象モデルがGameではない時(= 検索対象モデルがGenreの時) -->
          <% else %>
            検索ワード "<%= @word %>"
          <% end %>
        </h2>
        <thead>
          <tr>
            <th>ゲーム画像</th>
            <th>ゲーム名</th>
            <th>ジャンル名</th>
          </tr>
        </thead>
        <tbody>
          <% @games.each do |game| %>
            <tr>
              <td><%= image_tag game.get_profile_image(100, 100) %></td>
              <td>
                <%= link_to show_game_path(game.id) do %>
                  <%= game.name %>
                <% end %>
              </td>
              <td><%= game.genre.genre_name %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>